import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#2e7d32',
    },
    success: {
      main: '#2e7d32',
    },
  },
  shape: {
    borderRadius: 10,
  },
});

export default theme;
